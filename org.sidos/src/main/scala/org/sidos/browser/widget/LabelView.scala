package org.sidos.browser.widget
import org.kaivo.browser.scala.common._
import scala.swing._
import org.sidos.browser.common.ValueView

class LabelView(var value : Reference) extends Label with ValueView[Reference] {
  
}